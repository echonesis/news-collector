# 環境設定檔 - 安全範本
# 複製此檔案為 .env 並填入實際值
# ⚠️  請勿將 .env 檔案提交到 Git repository

# Email 模式 (mock 用於開發，real 用於生產)
EMAIL_MODE=mock

# Flask 設定
SECRET_KEY=your-secret-key-here  # 使用: openssl rand -base64 32 生成
DATABASE_URL=sqlite:///news_collector.db

# SMTP 郵件設定 (使用 Gmail 為例)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your-email@gmail.com
SENDER_PASSWORD=your-app-password  # Gmail 應用程式密碼，非一般密碼

# API 設定
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=True
